<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cetecom.ibichos">

    <!-- Permisos solicitados para GPS y Cámara -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".IBichosApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo_oficial"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_oficial"
        android:supportsRtl="true"
        android:theme="@style/Theme.IBichos">

        <!--
            Single Activity Architecture con Jetpack Compose.
            Solo existe MainActivity — toda la navegación ocurre en Compose NavHost.
        -->
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.IBichos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
